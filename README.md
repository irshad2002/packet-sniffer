# Packet Sniffer

A Python-based packet sniffer using Scapy. This project captures network packets and analyzes them.

## Requirements

- Python 3.x
- Scapy

## How to Use

1. Install the required libraries:
   ```bash
   pip install scapy

#Run the script

sudo python3 packet_sniffer.py
